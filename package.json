{
  "name": "product-management-backend",
  "version": "1.0.0",
  "description": "Simple CRUD API for product management",
  "main": "server.js",
  "scripts": {
  "start": "node ./server/server.js",
  "build": "echo 'No build step needed' && exit 0",
  "dev": "nodemon ./server/server.js",
  "postinstall": "cd product-management-frontend && npm install --save-dev && npm run build && cp -r dist ../server/frontend-dist && mkdir -p /var/app/current && cp -r ../ /var/app/current/"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "@vue/cli-plugin-babel": "^5.0.0"
  }
}

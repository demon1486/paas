{
  "name": "product-management-backend",
  "version": "1.0.0",
  "description": "Simple CRUD API for product management",
  "main": "server.js",
  "scripts": {
  "start": "node ./server/server.js",
  "build": "cd product-management-frontend && npm install --save-dev && npm run build && cp -r dist ../server/frontend-dist && sudo mkdir -p /var/app/current && sudo cp -r ../ /var/app/current/",
  "postinstall": "npm run build",
  "dev": "nodemon ./server/server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "@vue/cli-plugin-babel": "^5.0.0"
  }
}

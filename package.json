{
  "name": "product-management-backend",
  "version": "1.0.0",
  "description": "Simple CRUD API for product management",
  "main": "server.js",
  "scripts": {
    "start": "node ./server/server.js",
    "build": "cd product-management-frontend && npm ci --omit=dev && npm run build && cp -r dist ../server/frontend-dist",
    "dev": "nodemon ./server/server.js",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
